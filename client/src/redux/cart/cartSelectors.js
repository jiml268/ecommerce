export const selectCartID = (state) => state.cart.cartID;
export const selectCurrentCart = (state) => state.cart.currentCart;
export const selectAllCartItems = (state) => state.cart.allCartItems;
export const selectAllCartImages = (state) => state.cart.allCartImages;
